# ============================================================
# Dependências do Projeto: Análise e Previsão de Commodities
# ============================================================
#
# INSTALAÇÃO (recomendada):
#
# 1. Instalar PyTorch (CPU-only, compatível com Python 3.12+):
#    pip install torch --index-url https://download.pytorch.org/whl/cpu
#
# 2. Instalar demais dependências:
#    pip install -r requirements.txt
#
# OU instalar tudo de uma vez:
#    pip install torch --index-url https://download.pytorch.org/whl/cpu && pip install -r requirements.txt
# ============================================================

yfinance>=0.2.40
pandas>=2.0.0
numpy>=1.26.0
matplotlib>=3.7.0
seaborn>=0.13.0
scikit-learn>=1.3.0
statsmodels>=0.14.0
torch>=2.2.0
scipy>=1.11.0
